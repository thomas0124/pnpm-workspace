name: Deploy Preview Environment

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

env:
  PNPM_VERSION: "9.15.5"
  NODE_VERSION: "20"
  CLOUDFLARE_WORKERS_SUBDOMAIN: "sekibun3109"

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    name: Deploy Preview to Cloudflare
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm run build

      - name: Setup Cloudflare Wrangler
        run: pnpm add -g wrangler@3

      - name: Generate database migrations
        working-directory: packages/backend
        run: |
          echo "📝 Checking for schema changes and generating migrations..."
          pnpm run db:generate
          
          # 生成されたマイグレーションファイルを確認
          if [ -d "drizzle/migrations" ] && [ "$(ls -A drizzle/migrations/*.sql 2>/dev/null)" ]; then
            echo "✅ Migrations generated/updated"
            ls -la drizzle/migrations/
          else
            echo "⚠️ No migrations generated (schema may be unchanged)"
          fi

      - name: Create or get D1 database
        id: d1-setup
        working-directory: packages/backend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          # PR番号を使った一意のデータベース名
          DB_NAME="ar-pamph-db-pr-${{ github.event.pull_request.number }}"
          echo "DB_NAME=$DB_NAME" >> $GITHUB_OUTPUT

          # 既存のデータベースをチェック
          DB_LIST=$(wrangler d1 list --json || echo "[]")
          DB_ID=$(echo "$DB_LIST" | jq -r ".[] | select(.name==\"$DB_NAME\") | .uuid" || echo "")

          if [ -z "$DB_ID" ]; then
            echo "Creating new D1 database: $DB_NAME"
            wrangler d1 create "$DB_NAME"
            # データベース作成の反映を待つ
            sleep 2
            # リストから新しく作成されたデータベースのIDを取得
            DB_ID=$(wrangler d1 list --json | jq -r ".[] | select(.name==\"$DB_NAME\") | .uuid")
            echo "Created database with ID: $DB_ID"
          else
            echo "Using existing D1 database: $DB_NAME (ID: $DB_ID)"
          fi

          echo "DB_ID=$DB_ID" >> $GITHUB_OUTPUT

      - name: Update wrangler.toml with D1 database ID
        working-directory: packages/backend
        run: |
          # Preview環境のdatabase_idを動的に設定
          sed -i 's/database_id = ""/database_id = "${{ steps.d1-setup.outputs.DB_ID }}"/' wrangler.toml
          cat wrangler.toml

      - name: Run D1 Database Migrations
        working-directory: packages/backend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          # マイグレーションファイルが存在するかチェック
          if [ -d "drizzle/migrations" ] && [ "$(ls -A drizzle/migrations/*.sql 2>/dev/null)" ]; then
            echo "📦 Applying database migrations..."
            wrangler d1 migrations apply ${{ steps.d1-setup.outputs.DB_NAME }} --remote --env preview
            echo "✅ Migrations applied successfully"
          else
            echo "⚠️ No migrations found, skipping..."
          fi

      - name: Deploy Backend to Cloudflare Workers
        id: deploy-backend
        working-directory: packages/backend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          # Preview環境にデプロイ
          DEPLOY_OUTPUT=$(wrangler deploy --env preview)
          echo "$DEPLOY_OUTPUT"

          # 環境変数とWorker名から明示的にURLを生成
          WORKER_NAME="ar-pamph-preview"
          WORKER_URL="https://${WORKER_NAME}.${CLOUDFLARE_WORKERS_SUBDOMAIN}.workers.dev"
          
          echo "WORKER_URL=$WORKER_URL" >> $GITHUB_OUTPUT
          echo "✅ Backend deployed to: $WORKER_URL"

      - name: Build Frontend
        working-directory: packages/frontend
        env:
          NEXT_PUBLIC_API_URL: ${{ steps.deploy-backend.outputs.WORKER_URL }}
        run: |
          pnpm run build

      - name: Deploy Frontend to Cloudflare Pages
        id: deploy-frontend
        working-directory: packages/frontend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          # Cloudflare Pagesにデプロイ
          npx wrangler pages deploy .next --project-name=ar-pamph --branch=preview-pr-${{ github.event.pull_request.number }}

          # デプロイされたURLを生成
          PAGES_URL="https://preview-pr-${{ github.event.pull_request.number }}.ar-pamph.pages.dev"
          echo "PAGES_URL=$PAGES_URL" >> $GITHUB_OUTPUT
          echo "Frontend deployed to: $PAGES_URL"

      - name: Comment PR with deployment URLs
        uses: actions/github-script@v7
        env:
          FRONTEND_URL: ${{ steps.deploy-frontend.outputs.PAGES_URL }}
          BACKEND_URL: ${{ steps.deploy-backend.outputs.WORKER_URL }}
          DB_NAME: ${{ steps.d1-setup.outputs.DB_NAME }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const frontendUrl = process.env.FRONTEND_URL;
            const backendUrl = process.env.BACKEND_URL;
            const dbName = process.env.DB_NAME;

            const comment = `## 🚀 Preview Environment Deployed!

            ### 📱 Frontend
            ${frontendUrl}

            ### ⚙️ Backend API
            ${backendUrl}

            ### 🗄️ Database
            D1 Database: \`${dbName}\`

            ---

            Preview環境が正常にデプロイされました！
            各URLにアクセスして動作を確認してください。

            *このPreview環境はPRがマージまたはクローズされるまで利用可能です。*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

