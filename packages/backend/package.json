{
  "name": "@uttk/backend",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "import": "./index.ts",
      "types": "./index.ts"
    }
  },
  "scripts": {
    "dev": "tsx watch server.ts",
    "build": "tsc -p tsconfig.json",
    "deploy": "pnpm run deploy:production",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "lint:check": "eslint .",
    "lint:fix": "eslint . --fix",
    "format:check": "prettier --check .",
    "format:fix": "prettier --write .",
    "type-check": "tsc --noEmit",
    "check": "pnpm run type-check && pnpm run lint:check && pnpm run format:check",
    "fix": "pnpm run lint:fix && pnpm run format:fix",
    "generate-types": "wrangler types",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply ar-pamph-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply ar-pamph-db --remote",
    "db:migrate:remote:preview": "wrangler d1 migrations apply ar-pamph-db-preview --remote --env preview",
    "db:migrate:remote:staging": "wrangler d1 migrations apply ar-pamph-db-staging --remote --env staging",
    "db:migrate:remote:production": "wrangler d1 migrations apply ar-pamph-db-production --remote --env production",
    "db:studio": "drizzle-kit studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251121.0",
    "@hono/eslint-config": "^1.0.3",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20",
    "better-sqlite3": "^12.5.0",
    "drizzle-kit": "^0.31.7",
    "eslint": "^9",
    "eslint-config-prettier": "^9",
    "eslint-plugin-simple-import-sort": "^12",
    "prettier": "^3.2.5",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "wrangler": "^4.51.0"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.6",
    "uuid": "^13.0.0",
    "zod": "^4.1.13"
  }
}
